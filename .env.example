# Placeholders only — never fill in real secrets here.
BEE_URL=http://localhost:1633
# Identity that funds storage (public address of the node wallet/batch owner). Leave empty.
PAYER_IDENTITY=
# Publisher private key that signs the content feed. Leave empty.
PUBLISHER_IDENTITY=
# Succession authority private key that signs the root feed. Leave empty.
SUCCESSION_AUTHORITY_IDENTITY=
# Existing postage batch id that storage extension targets. Leave empty.
BATCH_ID=